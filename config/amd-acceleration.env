# SPDX-License-Identifier: GPL-3.0-only
# Opt-in AMD acceleration installation settings.
# Defaults follow AMD's current Ubuntu 24.04/noble ROCm package-manager layout;
# override these values locally when AMD publishes a newer supported Ubuntu codename
# or when testing a newer ROCm release.

ROCM_VERSION="7.2.4"
ROCM_REPO_CODENAME="noble"
ROCM_PACKAGES="rocm rocm-hip-runtime rocm-hip-sdk rocm-ml-sdk rocm-opencl-sdk amdgpu-lib"
ROCM_INSTALL_MODE="online"

# Ryzen AI Linux packages must be downloaded from AMD account/download pages and
# staged locally before this toolkit can install them. Paths are relative to the
# repository root unless absolute.
AMD_ARTIFACT_ROOT=".ai370-ai/amd-artifacts"
RYZEN_AI_ARTIFACT_GLOB="ryzen_ai-*.tgz"
# Accept the official Ryzen AI split XRT packages and the monolithic/source-built
# XDNA driver package names commonly produced by AMD/XRT build recipes.
XRT_DEB_GLOBS="xrt_*_24.04-amd64-base.deb xrt_*_24.04-amd64-base-dev.deb xrt_*_24.04-amd64-npu.deb xrt_*_24.04-amd64-xrt.deb xrt_plugin.*_24.04-amd64-amdxdna.deb xrt_plugin.*_ubuntu24.04-x86_64-amdxdna.deb"
RYZEN_AI_INSTALL_ROOT=".ai370-ai/ryzen-ai"

# ComfyUI launcher behavior after acceleration installation/validation.
COMFYUI_ACCELERATION_MODE="auto"
